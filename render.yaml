services:
  - type: web
    name: pyconsole-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn proxy:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GEMINI_CALO_API_KEYS
        sync: false